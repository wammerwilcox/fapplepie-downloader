# 2.0.0 Scraper Hardening Status

## Core Goal

Make scraper failures fast to diagnose and safer to operate while keeping the current lightweight HTTP and `curl_cffi` approach. This release does not add Playwright, Selenium, browser automation, CAPTCHA solving, or broad CLI redesign.

## Scope

Included:

- `--probe` mode for diagnosing the scrape path without downloads or URL/cache mutation.
- Phase-aware diagnostics for base URL probing, robots handling, first-page fetch/parse, and one production-style redirect resolution.
- Polite timing controls with scheduled-run start jitter and page/redirect delays.
- Transport isolation around scrape requests so future `curl_cffi` or proxy changes stay local to scrape transport code.
- Docs and tests for the diagnostic and timing behavior.

Excluded:

- Browser automation.
- CAPTCHA or challenge solving.
- Cookie/session persistence beyond existing scraper headers/session behavior.
- Major CLI redesign beyond targeted flags and configuration.

## Implemented Checkpoints

- Probe design and implementation:
  - `dc58733 docs: design probe mode for 2.0.0`
  - `d394f8c docs: plan probe mode implementation`
  - `5a7b5d0 feat: add probe result types`
  - `95b6dd0 feat: add scraper probe workflow`
  - `49daebd test: cover scraper probe failures`
  - `2a713c6 feat: add probe cli dispatch`
  - `67af005 docs: document probe mode`
- Transport and diagnostics:
  - `f1660e5 refactor: extract scrape link parsing`
  - `6ce773d fix: preserve pagination after link parser extraction`
  - `0def3a8 fix: stabilize Fapplepie scrape transport`
  - Fapplepie HTML requests use the proven HTTP/1.1 profile while unrelated
    download targets retain their existing transport settings.
  - Direct fallback only follows a failed proxied request or proxied `403`.
  - Probe redirect resolution uses the same bounded redirect resolver as a
    normal scrape.
- Polite timing:
  - `dc8a4de feat: add polite scraper timing`
  - Scheduled jitter is applied through `daily_download.sh` and `entrypoint.sh`.
  - Page and redirect pacing are controlled by `SCRAPE_DELAY_SECONDS`, `SCRAPE_DELAY_JITTER_SECONDS`, `SCRAPE_REDIRECT_DELAY_SECONDS`, and `SCRAPE_REDIRECT_DELAY_JITTER_SECONDS`.
- YouTube runtime/auth follow-up:
  - `7f0648f fix: support YouTube auth runtime`
  - `7fb9bef Ensure cookies are read/write mounted`
- Cron observability:
  - `b5f0d8f feat: add cron dispatch observability`
  - Scheduled runs persist their dispatch, completion, and exit status.
  - Download failures return a non-zero process status and remain out of the
    downloaded cache.

## Deferred after 2.0.0

- Make the scrape page limit configurable if the fixed 100-page safety limit
  proves too low in operation.
- Add bounded `yt-dlp` retry/backoff only after collecting real transient and
  permanent failure output to classify safely.

## Branch Strategy

The active long-lived integration branch is:

- `codex/2.0.0-scraper-hardening`

Small focused changes should continue landing on this branch or short-lived branches off it, then be verified before the final 2.0.0 PR to `main`.
